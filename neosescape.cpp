#include <bits/stdc++.h>
using namespace std;
int main(){
    string s;
    cin>>s;
    map<string, string> dic;
    dic.insert(make_pair("b", "d"));
    dic.insert(make_pair("p", "q"));
    dic.insert(make_pair("6", "9"));
    for (int i=0; i<size(s); i++){
        if (dic.find(s[i]))
        
    }
}